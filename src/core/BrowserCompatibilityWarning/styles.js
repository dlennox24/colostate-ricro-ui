const styles = theme => ({
  browserPaper: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    padding: theme.spacing(3),
  },
  contentContainer: {
    paddingTop: theme.spacing(2),
  },
});

export default styles;
