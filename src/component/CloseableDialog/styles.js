const styles = theme => ({
  dialogTitleRoot: {
    padding: 0,
  },
  dialogRoot: {
    width: '100%',
  },
  dialogContent: {
    padding: theme.spacing(3),
  },
  flex: {
    flex: 1,
  },
  headerGutter: {
    marginBottom: theme.spacing(3),
  },
});

export default styles;
